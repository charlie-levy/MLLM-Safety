# Review-only image sets, regenerated by build_review.py -- never commit these.
# siuo_images/ is the benchmark source set pulled from Newton (~200MB);
# images/ is the generated clean+corrupted pairs (~110MB).
siuo_images/
images/
